import { ForgretPasswordRequest } from './forgret-password-request';

describe('ForgretPasswordRequest', () => {
  it('should create an instance', () => {
    expect(new ForgretPasswordRequest()).toBeTruthy();
  });
});
